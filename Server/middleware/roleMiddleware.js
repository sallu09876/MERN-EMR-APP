import { authorizeRoles } from "./authMiddleware.js";

// Simple re-export to keep a clear separation of concerns and match folder structure
// Usage: authorizeRoles("SUPER_ADMIN"), etc.
export { authorizeRoles };

